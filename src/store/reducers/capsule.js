const INITIAL_STATE = {};

const reducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    case "GET_CAPSULES":
      return {};

    case DECREMENT:
      return {};

    default:
      return state;
  }
};
